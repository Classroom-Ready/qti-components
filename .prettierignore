.cache
.github
.vscode
dist
node_modules
package-lock.json
pnpm-lock.yaml
tsconfig.json

public/assets
# `pnpm install` regenerates this worker via msw's postinstall, in msw's own
# style. Formatting it makes every install dirty the working tree, so it is
# committed exactly as msw writes it — see AGENTS.md.
public/mockServiceWorker.js
LICENSE.md
QTI-Components.code-workspace
