---
'@qti-components/associate-interaction': patch
'@qti-components/theme': minor
---

- **qti-theme**: add `qti-rubric-block` element styles and register them in the theme's element imports.
- **associate-interaction**: switch associable choices from `slot` to the `part` attribute so they can be styled from outside the shadow DOM.
